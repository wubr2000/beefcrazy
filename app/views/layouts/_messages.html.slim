- flash.each do |name, message|
  - if message.is_a?(String)
    - noty_type = (name == :notice ? "success" : "error")
    - timeout = (name == :notice ? 4000 : false)
    javascript:
      $(function() {
        noty({
          layout: 'center',
          type: "#{noty_type}",
          text: '#{message}',
          timeout: "#{timeout}"
        });
      });